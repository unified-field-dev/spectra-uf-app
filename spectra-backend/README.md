# spectra-backend

Pure schema catalog and explore-query stub helpers used by `spectra-app` server
functions. Kept free of Leptos/UI deps so Layer 1 verification can run when the
host product UI graph is unavailable.

See [`docs/VERIFICATION.md`](../docs/VERIFICATION.md).
